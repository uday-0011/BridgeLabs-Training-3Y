ASSIGNMENT 7 - CSS SOLUTIONS

10 separate solutions are included.
Each question has:
  index.html
  style.css

No JavaScript or external frameworks are used.
Semantic HTML5, Flexbox/Grid, responsive CSS, transitions, selectors,
gradients, transforms, typography, box model, outlines, and navigation
requirements are demonstrated as specified in the assignment.

Some visual demos use placehold.co for a background image. Replace those
URLs with local images if an offline-only submission is required.
